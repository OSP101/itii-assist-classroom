# =============================================================================
# Prometheus Config - DEV SERVER
# Scrapes backend via Docker container name (itii-dev-backend)
# Prometheus + Alertmanager อยู่ใน itii-network เดียวกับ backend
# =============================================================================

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

rule_files:
  - "alert.rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "itii-alertmanager-dev:9093"

scrape_configs:
  # Prometheus self
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Backend (Docker container บน itii-network)
  - job_name: "backend"
    metrics_path: "/api/metrics/prometheus"
    scrape_interval: 10s
    static_configs:
      - targets: ["itii-dev-backend:3001"]
        labels:
          service: "backend"
          environment: "development"

  # Loki self-monitoring
  - job_name: "loki"
    static_configs:
      - targets: ["itii-loki-dev:3100"]
        labels:
          service: "loki"
